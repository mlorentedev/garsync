# CLAUDE.md

> **Read [`AGENTS.md`](AGENTS.md) first** — it is the canonical source of truth for how AI coding
> agents work in this repo (stack, paths, commands, workflow rules, Spec-Driven Development, the
> review gates in `harness/`). This file holds only Claude Code-specific notes layered on top of it.
>
> If a rule here ever conflicts with `AGENTS.md`, **`AGENTS.md` wins** for behaviour. Keep this file
> a thin pointer: durable rules belong in `AGENTS.md`, so every agent (Claude, Copilot, Cursor,
> Codex, Gemini) sees them.

## Claude Code notes

<!-- Add Claude-specific tooling notes here (skills, MCP servers, hooks). -->
